# forge — per-developer claude-code config (machine-local, not committed)
.claude/settings.local.json
.claude/commands/

# Rust build artifacts
/target/

# Local credentials / config — NEVER commit (contains Trakt client secret)
/config.toml
config.local.toml
tokens.json
